{
  "schemaVersion": 1,
  "id": "raid-reporter",
  "version": "${version}",

  "name": "Raid Reporter",
  "description": "Broadcast your guild raids to discord via webhook!",
  "authors": [
    "otcathatsya"
  ],
  "contact": {
    "homepage": "https://cath.at/",
    "sources": "https://github.com/otcathatsya/wynn-raid-reporter"
  },

  "license": "CC0-1.0",
  "icon": "assets/imp/icon.png",

  "environment": "*",
  "entrypoints": {
    "main": [
      {
        "adapter": "kotlin",
        "value": "at.cath.RaidReporter"
      }
    ]
  },
  "mixins": [
    "imp.mixins.json"
  ],

  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-api": "*",
    "minecraft": "~${minecraft_version}",
    "java": ">=21",
    "fabric-language-kotlin": ">=${fabric_kotlin_version}",
    "cloth-config2": ">=${cloth_config_version}"
  }
}
